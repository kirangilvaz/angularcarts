angular.module("amplify.demo")
	.controller("DemoController", [
		"$scope",
        "$interval",
        "$timeout",
    	function($scope, $interval, $timeout) {
            $scope.kiran = "kiran";

    	}
    ]
);
